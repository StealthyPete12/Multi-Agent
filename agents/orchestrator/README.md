# Orchestrator (placeholder)

**Status:** not implemented — scaffolded in Phase 0 for repo structure only.

Will own queue/exchange topology, dispatch work across analysis agents,
track per-commit review state, and handle retries/timeouts for agents that
never publish `FindingsReady`.
